# Adaptive VolControl Fund 대시보드 - 의존성 (J-5: Range versions)
# 참조: docs/plan/01_setup.md 3.2 / decisionlog/11_dl_sections.md J 섹션
#
# Major version 핀 = 메이저 변경 회피
# Minor 범위 = 보안 패치 자동 수용
# 설치: pip install -r requirements.txt

# === 메인 프레임워크 ===
streamlit>=1.30,<2.0

# === 차트 ===
plotly>=6.0,<7.0

# === 데이터 처리 ===
# numpy 2.x 강제: pickle 데이터가 numpy 2.x로 생성됨 (binary 포맷 NEP 50)
pandas>=2.0,<3.0
numpy>=2.0,<3.0

# === 외부 데이터 (1회 수집용 - scripts/ 에서만) ===
yfinance>=0.2,<1.0

# === 통계 / 회귀 ===
scipy>=1.11,<2.0
statsmodels>=0.14,<1.0

# === UI 컴포넌트 ===
streamlit-card>=1.0,<2.0
